spring:
 datasource:
  url: jdbc:postgresql://localhost:5434/vjshow
  username: postgres
  password: 123456
 jpa:
  hibernate:
   ddl-auto: update
  show-sql: true
  properties:
   hibernate:
    dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
   init:
   mode: never